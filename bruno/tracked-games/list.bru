meta {
  name: List tracked games
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/tracked-games
  body: none
  auth: inherit
}

tests {
  test("list returns 200", function() {
    expect(res.status).to.equal(200);
  });
}
